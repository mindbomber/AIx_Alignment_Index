# Notebooks

`example_assessment.ipynb` is a reproducible demonstration built from the public `aix`
API. Generated notebook output is not validation evidence unless the underlying dataset,
rater protocol, and analysis are published.
