# AIx Web

React and Vite interface for the AIx Platform API.

```bash
npm ci
npm run dev
npm run lint
npm run build
npm run test:e2e
```

Set `VITE_API_URL` when the API is not available at `http://localhost:8000`.
The end-to-end tests expect the repository's `scripts/seed_e2e.py` data.
